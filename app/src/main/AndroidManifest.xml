<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tuojin.tvfilm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".modules.main.MainActivity"/> > -->

        <activity
            android:name=".modules.main.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 电影详情 -->
        <activity android:name=".modules.main.FilmDetailActivity"/>
        <!-- 搜索界面 -->
        <activity android:name=".modules.search.SearchActivity"/>
        <activity android:name=".modules.catelist.CateListActivity"/>
        <activity android:name=".modules.catelist.CategoryGeneralActivity"/>
    </application>

</manifest>