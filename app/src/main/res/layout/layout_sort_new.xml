<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="182dp"
              android:background="@color/black"
              android:layout_height="match_parent"
              android:orientation="vertical">

    <LinearLayout
        android:id="@+id/tab_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="30dp"
        android:orientation="vertical">
        <ImageButton
            android:id="@+id/iv_back"
            android:layout_marginStart="64dp"
            android:background="@drawable/iv_back_selector"
            android:layout_marginLeft="30dp"
            android:layout_width="36dp"
            android:layout_height="36dp" />
        <RadioButton
            android:id="@+id/tab_indicator_new"
            style="@style/Widget.TabIndicator"
            android:text="最新" />

        <RadioButton
            android:id="@+id/tab_indicator_hot"
            style="@style/Widget.TabIndicator"
            android:text="最热" />

        <RadioButton
            android:id="@+id/tab_indicator_big"
            style="@style/Widget.TabIndicator"
            android:text="大片" />

        <RadioButton
            android:id="@+id/tab_indicator_ad"
            style="@style/Widget.TabIndicator"
            android:text="广告" />

        <RadioButton
            android:id="@+id/tab_indicator_douban"
            style="@style/Widget.TabIndicator"
            android:text="豆瓣" />
    </LinearLayout>
</LinearLayout>